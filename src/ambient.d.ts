// The Microsoft SDKs are optional peer dependencies, loaded via dynamic import.
// Declare them as untyped so the kit compiles standalone (without them installed).
declare module "@microsoft/opentelemetry";
declare module "@microsoft/agents-hosting";
